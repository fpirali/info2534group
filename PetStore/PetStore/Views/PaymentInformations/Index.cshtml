@model IEnumerable<PetStore.Models.PaymentInformation>

@{
    ViewBag.Title = "Index";
}

<h2>Checkout</h2>

<!--<p>
    @Html.ActionLink("Create New", "Create")
</p>-->
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CardNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpirationMonth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpirationYear)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CardPin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CardNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpirationMonth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpirationYear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CardPin)
        </td>
 <!--       <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>-->
    </tr>
}

</table>
